# Dependabot dummy project

This is a project in order to test how to configure dependabot, so it doesn't crawl the `pip-tools` generated lockfiles
and suggests updates based only on `dev.in` and `main.in`.
